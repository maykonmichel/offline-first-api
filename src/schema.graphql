schema {
    query: Query
}

type Mutation {
    addTodo(input: todoInput): Todo!
}

type Query {
    todoList: [Todo!]!
}

type Todo {
    id: ID!
    description: String!
}

input todoInput {
    description: String!
}
